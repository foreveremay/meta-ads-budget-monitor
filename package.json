{
    "name": "meta-ads-monitor-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd server && npm install",
        "build": "cd server && npx prisma generate && npm run build",
        "start": "cd server && npx prisma migrate deploy && npm start"
    },
    "engines": {
        "node": "20.x"
    }
}